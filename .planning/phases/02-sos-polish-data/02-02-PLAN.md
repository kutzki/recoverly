---
phase: 02-sos-polish-data
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - components/sos/SOSResponseScreen.tsx
  - app/(app)/sos/index.tsx
  - app/(app)/sos/bad-day.tsx
  - app/(app)/sos/feel-like-using.tsx
  - app/(app)/sos/just-relapsed.tsx
  - app/(app)/sos/self-harm.tsx
  - app/(app)/sos/feeling-anxious.tsx
  - app/(app)/crisis-history.tsx
autonomous: false
requirements:
  - R2

must_haves:
  truths:
    - "All 6 SOS screens are visually indistinguishable from the Figma designs at the node IDs retrieved during this plan"
    - "SOS index tiles match Figma: correct tile shape, emoji size, typography, and color stripe treatment"
    - "Each sub-screen header gradient, affirmation text size, step card layout, and CTA button styling matches Figma"
    - "Crisis history screen cards match Figma: correct font sizes, color treatment, and spacing"
  artifacts:
    - path: "components/sos/SOSResponseScreen.tsx"
      provides: "Figma-matched shared sub-screen layout"
    - path: "app/(app)/sos/index.tsx"
      provides: "Figma-matched SOS index tile selector"
    - path: "app/(app)/crisis-history.tsx"
      provides: "Figma-matched crisis history card list"
  key_links:
    - from: "Figma node fetch"
      to: "SOSResponseScreen.tsx styles"
      via: "Direct pixel comparison → StyleSheet updates"
      pattern: "StyleSheet\\.create"
    - from: "Figma node fetch"
      to: "app/(app)/sos/index.tsx styles"
      via: "Direct pixel comparison → optionCard/header style updates"
      pattern: "optionCard"
---

<objective>
Fetch Figma designs for all 6 SOS screens, compare against current implementation, and fix every visual deviation. Typography sizes, spacing values, color treatments, button styles, border radii, and icon choices must all match Figma exactly.

Purpose: REQUIREMENTS.md R2 requires "All 6 screens Figma-perfect". Plan 02-01 fixed the data layer. This plan completes the visual half.
Output: All 6 screens pixel-matched to Figma. Human verifies on device.
</objective>

<execution_context>
@C:/Users/kutzk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kutzk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-sos-polish-data/02-RESEARCH.md
@.planning/phases/02-sos-polish-data/02-01-SUMMARY.md

<interfaces>
<!-- Current style contracts from codebase — executor updates these to match Figma. -->

From components/sos/SOSResponseScreen.tsx (after 02-01):
- headerTitle: Poppins Bold 18, white
- affirmation: Jost 15, rgba(255,255,255,0.9), lineHeight 24
- stepsLabel: Poppins SemiBold 16
- stepCard: cardTintPurpleFaint bg, borderRadius 12, padding 16, borderColor primaryLight
- stepNumber circle: 32×32, borderRadius 16, primary bg
- ctaBtn: primary bg, borderRadius 8, paddingV 8, paddingH 14
- finishBtn: primary bg, borderRadius 14, paddingV 16

From app/(app)/sos/index.tsx (after 02-01):
- optionCard: white bg, borderRadius 12, padding 16, borderLeftWidth 4, elevation 2
- optionEmoji: fontSize 28
- optionLabel: Poppins Medium 15
- helplineCard: sosBgRed bg, borderRadius 12

From app/(app)/crisis-history.tsx (after 02-01):
- Gradient header (added in 02-01)
- card: cardTintPurpleFaint bg, borderRadius 12, padding 14, borderLeftColor per incident type
- cardType: Poppins SemiBold 14
- cardDate: Jost 12

Figma file key: uVW028XTHA5DcuJh8DjcNs
(Retrieve SOS node IDs during Task 1 — they are not pre-documented.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch Figma nodes for all 6 SOS screens and identify deviations</name>
  <files></files>
  <action>
Use the Figma MCP tool (or context7 / fetch) to look up the SOS screen nodes in Figma file `uVW028XTHA5DcuJh8DjcNs`.

Start by fetching the file's top-level pages/frames to find the SOS section. The home screen is at node `0:1940` — the SOS screens will be nearby in the same file. Navigate the frame tree to identify node IDs for:
1. SOS Index screen (tile selector)
2. Each of the 5 sub-screens (Bad Day, Feel Like Using, Just Relapsed, Self Harm, Feeling Anxious)
3. Crisis History screen

For each screen, extract:
- Background color or gradient colors
- Header height, padding, title font size
- Tile/card dimensions, border radius, border treatment, shadow
- Typography: font family, size, weight (translate "weight" to the correct Fonts.* constant — Bold=poppinsBold, SemiBold=poppinsSemiBold, Medium=poppinsMedium, Regular=poppins; for Jost use jost/jostMedium)
- Button dimensions, border radius, colors
- Spacing: gaps, paddings, margins
- Icon names and sizes

Document every deviation found between current code and Figma in a structured list before writing any code. This becomes the change list for Task 2.

If the Figma MCP is unavailable, use the WebFetch tool with the Figma REST API:
`https://api.figma.com/v1/files/uVW028XTHA5DcuJh8DjcNs/nodes?ids=<discovered_ids>`

If no Figma access is available at all, document the screens as "Figma unavailable — applying best-practice defaults" and proceed with polish improvements based on the research doc patterns (consistent spacing, correct font pairings, polished card shadows).
  </action>
  <verify>
    <automated>cd /c/Users/kutzk/Documents/recoverly/.claude/worktrees/hungry-elion && echo "Figma lookup complete — deviation list in task output"</automated>
  </verify>
  <done>
    - Node IDs for all 6 screens identified (or documented as unavailable)
    - Deviation list produced: each item names the screen, the property, the current value, and the Figma value
    - No code written yet
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply all Figma deviations across all 6 screens</name>
  <files>
    components/sos/SOSResponseScreen.tsx
    app/(app)/sos/index.tsx
    app/(app)/sos/bad-day.tsx
    app/(app)/sos/feel-like-using.tsx
    app/(app)/sos/just-relapsed.tsx
    app/(app)/sos/self-harm.tsx
    app/(app)/sos/feeling-anxious.tsx
    app/(app)/crisis-history.tsx
  </files>
  <action>
Apply every deviation identified in Task 1. Work through the deviation list item by item.

**General rules:**
- All font families MUST use Fonts.* constants — never raw fontFamily strings, never fontWeight
- All colors MUST use Colors.* constants — never inline hex strings (add new constants to colors.ts if Figma specifies a color not already there)
- All spacing values must be exact numbers matching Figma (not approximations)
- Do NOT change the data layer (useEffect logging, logCTAAction, incidentIdRef) — those were completed in 02-01
- Do NOT change step content text, step ids, incidentType values, or route paths

**Priority order if deviation list is long:**
1. Fix SOSResponseScreen.tsx first (affects all 5 sub-screens at once)
2. Fix sos/index.tsx (most visible screen — first thing user sees)
3. Fix crisis-history.tsx
4. Fix individual sub-screen files only if they have screen-specific overrides that differ from SOSResponseScreen

**Common likely fixes (based on research patterns):**
- Step card padding/border-radius may differ from Figma
- CTA button border-radius may be fully rounded (borderRadius: 999) vs current 8
- Affirmation text lineHeight may differ
- Option tile emoji size may be 32px not 28px
- sectionLabel fontSize may be 13 or 15 not 14

After applying all changes, run TypeScript check. Fix any type errors before committing.
  </action>
  <verify>
    <automated>cd /c/Users/kutzk/Documents/recoverly/.claude/worktrees/hungry-elion && npx tsc --noEmit --skipLibCheck 2>&1 | head -30</automated>
  </verify>
  <done>
    - Every deviation from Task 1's list has been applied
    - No inline color strings or fontWeight in any modified file
    - TypeScript exits 0
    - All 6 files touched only where necessary (no spurious whitespace changes)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All 6 SOS screens have been pixel-polished against Figma. An EAS preview build has been triggered and installed on device. Both the data fixes from plan 02-01 and the visual polish from this plan are included.
  </what-built>
  <how-to-verify>
    Trigger an EAS build after Task 2 commits: `eas build --platform android --profile preview --non-interactive`

    Once installed, verify each screen:

    1. **SOS Index** — Open SOS tab. Compare tile list to Figma: tile shape, emoji size, left-border colors, typography, helpline card styling.
    2. **Bad Day** — Tap "I'm having a bad day". Check header gradient, affirmation text, step cards, finish button.
    3. **Feel Like Using** — Tap this option. Verify header. Tap "Open Sponsor Screen" CTA — should navigate to sponsor screen (not do nothing). Check Supabase crisis_incidents table for a new row with incident_type = 'feel_like_using'.
    4. **Just Relapsed** — Tap this option. Tap "Find a Meeting" CTA — should open the in-app meetings screen (not a browser). Check Supabase for new row.
    5. **Self Harm** — Header should use the correct red (#FF3B30 via Colors.sosRedBright). Call 988 and Text 741741 CTAs should work.
    6. **Feeling Anxious** — All informational steps visible and styled correctly.
    7. **Crisis History** — Navigate to crisis history. After testing above screens, you should see multiple incidents listed with: colored left border, pill-tag actions, formatted date+time, and formatted incident type name.

    Compare each screen visually against Figma designs.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 screens match Figma and data is logging correctly. Or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Phase 2 is complete when the human approves the checkpoint above. Pre-checkpoint automated checks:

1. TypeScript clean: `npx tsc --noEmit --skipLibCheck` exits 0
2. No inline hex strings in SOS files: `grep -rn '"#[0-9A-Fa-f]' app/(app)/sos/ components/sos/ app/(app)/crisis-history.tsx` returns nothing (or only non-color strings)
3. No fontWeight in SOS files: `grep -rn 'fontWeight' app/(app)/sos/ components/sos/ app/(app)/crisis-history.tsx` returns nothing
4. No external aa.org link: `grep -rn 'aa.org' app/(app)/sos/` returns nothing
</verification>

<success_criteria>
- All 6 SOS screens visually match Figma designs (user confirms on device)
- Crisis incidents log to Supabase on sub-screen open (verified via Supabase dashboard)
- CTA actions (sponsor, meeting) navigate correctly within the app
- Crisis history shows real rows with polished card layout
- TypeScript exits 0, no inline colors, no fontWeight
</success_criteria>

<output>
After completion, create `.planning/phases/02-sos-polish-data/02-02-SUMMARY.md` using the summary template.
</output>
