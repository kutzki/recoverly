---
phase: 01-dashboard-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/SobrietyCounter.tsx
  - components/ui/StreakDots.tsx
autonomous: true
requirements:
  - R1

must_haves:
  truths:
    - "Arc label reads 'days sober' in lowercase matching Figma spec"
    - "StreakDots renders day abbreviations (M T W T F S S) below each dot"
    - "TypeScript check passes with no errors after changes"
  artifacts:
    - path: "components/ui/SobrietyCounter.tsx"
      provides: "Arc label lowercase 'days sober', no textTransform"
    - path: "components/ui/StreakDots.tsx"
      provides: "Day abbreviation labels below each dot using Fonts.poppins 9px Colors.textMuted"
  key_links:
    - from: "components/ui/StreakDots.tsx"
      to: "constants/fonts.ts"
      via: "Fonts.poppins import"
      pattern: "Fonts\\.poppins"
    - from: "components/ui/SobrietyCounter.tsx"
      to: "label style"
      via: "string literal and style"
      pattern: "days sober"
---

<objective>
Fix the two remaining pixel-polish deviations between the live home screen and Figma node 0:1940: the arc label casing and the missing day abbreviations under the streak dots.

Purpose: These are the last two visual gaps preventing R1 completion. Both are surgical single-file edits.
Output: SobrietyCounter renders lowercase label; StreakDots renders M/T/W/T/F/S/S abbreviations below dots.
</objective>

<execution_context>
@C:/Users/kutzk/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kutzk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@components/ui/SobrietyCounter.tsx
@components/ui/StreakDots.tsx
@constants/colors.ts
@constants/fonts.ts

<interfaces>
<!-- Current code state the executor must understand before editing. -->

SobrietyCounter.tsx — current label (line 70–71):
```typescript
<Text style={styles.label}>Days Sober</Text>
// ...
label: {
  fontFamily: Fonts.poppins,
  fontSize: 20,
  color: Colors.text,
  textTransform: 'capitalize',   // <-- REMOVE this
},
```
Fix: change string to `'days sober'` and remove `textTransform: 'capitalize'` from the style.

StreakDots.tsx — current structure (no labels):
```typescript
// index 0 = Monday, last = Sunday
// abbreviations: ['M', 'T', 'W', 'T', 'F', 'S', 'S']
```
Fix: wrap each dot in a column View, render the abbreviation Text below the dot.

From constants/fonts.ts:
```typescript
Fonts.poppins   // 'Poppins_400Regular'
```

From constants/colors.ts:
```typescript
Colors.textMuted   // 'rgba(0,0,0,0.5)'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix arc label casing in SobrietyCounter</name>
  <files>components/ui/SobrietyCounter.tsx</files>
  <action>
Two targeted changes to `components/ui/SobrietyCounter.tsx`:

1. **Change the label string** on the `<Text>` element (currently line 70):
   - FROM: `Days Sober`
   - TO: `days sober`

2. **Remove `textTransform: 'capitalize'`** from the `label` style (currently line 92):
   - The style block should have `fontFamily`, `fontSize`, `color` — no `textTransform` property at all.

No other changes. The `days` number style, the arc SVG, and all other styles remain unchanged.

Do NOT use `fontWeight`. Do NOT change colors or sizes.
  </action>
  <verify>
    <automated>cd /c/Users/kutzk/Documents/recoverly/.claude/worktrees/hungry-elion && npx tsc --noEmit --skipLibCheck 2>&1 | head -20</automated>
  </verify>
  <done>SobrietyCounter.tsx renders `'days sober'` (lowercase string) with no `textTransform` in the label style. TypeScript check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add day abbreviation labels to StreakDots</name>
  <files>components/ui/StreakDots.tsx</files>
  <action>
Extend `components/ui/StreakDots.tsx` to render day abbreviations (M T W T F S S) below each dot.

**Add import** for `Fonts` at the top (Colors is already imported):
```typescript
import { Fonts } from '../../constants/fonts';
```

**Add day abbreviations constant** before the component:
```typescript
const DAY_LABELS = ['M', 'T', 'W', 'T', 'F', 'S', 'S'];
```

**Wrap each dot** in a column `View` that contains the dot and a label below it. Replace the current `streak.map(...)` render with:
```typescript
{streak.map((done, i) => (
  <View key={i} style={styles.dotCol}>
    <View style={[styles.dot, done ? styles.dotFilled : styles.dotEmpty]}>
      {done && <Text style={styles.check}>✓</Text>}
    </View>
    <Text style={styles.dayLabel}>{DAY_LABELS[i]}</Text>
  </View>
))}
```

**Update the row style** — the outer `row` style stays `flexDirection: 'row', gap: 5`. The new `dotCol` style is:
```typescript
dotCol: {
  alignItems: 'center',
  gap: 3,
},
```

**Add dayLabel style**:
```typescript
dayLabel: {
  fontFamily: Fonts.poppins,
  fontSize: 9,
  color: Colors.textMuted,
},
```

Do NOT change dot size (DOT = 15), dot colors, check mark style, or the gap between dots. Do NOT use `fontWeight`.
  </action>
  <verify>
    <automated>cd /c/Users/kutzk/Documents/recoverly/.claude/worktrees/hungry-elion && npx tsc --noEmit --skipLibCheck 2>&1 | head -20</automated>
  </verify>
  <done>StreakDots.tsx renders M/T/W/T/F/S/S abbreviations below each dot using Fonts.poppins fontSize 9 Colors.textMuted. TypeScript check passes with no new errors.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx tsc --noEmit --skipLibCheck` produces no new TypeScript errors
2. `grep "days sober" components/ui/SobrietyCounter.tsx` finds the lowercase string
3. `grep "textTransform" components/ui/SobrietyCounter.tsx` returns nothing
4. `grep "DAY_LABELS" components/ui/StreakDots.tsx` finds the labels array
5. `grep "dayLabel" components/ui/StreakDots.tsx` finds both the style usage and definition
</verification>

<success_criteria>
- SobrietyCounter renders `'days sober'` with no textTransform — label matches Figma lowercase spec
- StreakDots renders 7 column items each with dot + abbreviation letter below
- All font usage is via Fonts.* named families — no fontWeight anywhere in modified files
- TypeScript check passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/01-dashboard-polish/01-02-SUMMARY.md`
</output>
